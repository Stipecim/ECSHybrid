#include<iostream>
// hybrid ECS

#include "Entity.h"
#include "EntityManager.h"

EntityManager manager;
auto& NewPlayer(manager.addEntity());

int main(int agrc, char* agrv[]) { // int main with parameter with ArgumentCounter and ArgumentVector that takes external arguments after execution

	// entity initialisation 

	while (true) {

		// Entity Update 
		break;
	}
	return 0;
}


